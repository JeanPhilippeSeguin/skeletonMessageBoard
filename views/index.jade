extends layout 
   

block content
  h1= title
  p Welcome to #{title}
  
  ul
    each message in messages
      li  #{message.user} 
      p #{message.text} 
      p #{message.added}

  a(href="/new", class="button") Post a message